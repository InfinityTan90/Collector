<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Collection.Pages.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Collection.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:Hinstance ="using:Collection.Global_var"
    mc:Ignorable="d">

    <Grid>
        <Grid>
            <ScrollViewer>

                <StackPanel Margin="36,0,36,0">
                    <TextBlock
                        Text="Settings"
                        Margin="0,40,0,0"
                        Style="{StaticResource TitleTextBlockStyle}"/>

                    <StackPanel 
                        Spacing="4"
                        Margin="0,36,0,0">

                        <TextBlock Text="Personalization" Margin="0,0,0,8"
                            Style="{StaticResource BodyStrongTextBlockStyle}"/>

                        <toolkit:SettingsCard
                            Header="App theme"
                            Description="Select a app theme to display.">
                            <toolkit:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE790;"/>
                            </toolkit:SettingsCard.HeaderIcon>

                            <ComboBox 
                                x:Name="AppThemeSelection"
                                SelectedIndex="{x:Bind Hinstance:HInstance.Theme}"
                                SelectionChanged="AppTheme_SelectionChanged">
                                <ComboBoxItem>Light</ComboBoxItem>
                                <ComboBoxItem>Dark</ComboBoxItem>
                            </ComboBox>

                        </toolkit:SettingsCard>

                        <toolkit:SettingsCard
                            Header="Language"
                            Description="Select a language to display.">
                            <toolkit:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE775;"/>
                            </toolkit:SettingsCard.HeaderIcon>

                            <ComboBox 
                                x:Name="AppLanguageSelection"
                                SelectedIndex="0">
                                <ComboBoxItem>English</ComboBoxItem>
                                <ComboBoxItem>中文</ComboBoxItem>
                            </ComboBox>
                        </toolkit:SettingsCard>

                    </StackPanel>

                    <StackPanel 
                        Spacing="4"
                        Margin="0,26,0,0">

                        <TextBlock Text="Help and feedback" Margin="0,0,0,8"
                            Style="{StaticResource BodyStrongTextBlockStyle}"/>

                        <toolkit:SettingsCard
                            Header="Feedback"
                            Description="Contact author."
                            IsClickEnabled="True">

                            <toolkit:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xED15;"/>
                            </toolkit:SettingsCard.HeaderIcon>
                        </toolkit:SettingsCard>

                    </StackPanel>


                    <StackPanel 
                        Spacing="4"
                        Margin="0,26,0,32">
    
                        <TextBlock Text="About" Margin="0,0,0,8"
                            Style="{StaticResource BodyStrongTextBlockStyle}"/>

                        <toolkit:SettingsExpander
                                Header="Collector"
                                Description="About">
                            <toolkit:SettingsExpander.HeaderIcon>
                                <FontIcon Glyph="&#xE8F2;"/>
                            </toolkit:SettingsExpander.HeaderIcon>

                            <toolkit:SettingsExpander.Items>
                                <toolkit:SettingsCard
                                    Header="Version :">
                                    <toolkit:SettingsCard.HeaderIcon>
                                        <FontIcon Glyph="&#xED35;"/>
                                    </toolkit:SettingsCard.HeaderIcon>
                                    <TextBlock 
                                    Text="0.8.0"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </toolkit:SettingsCard>

                                <toolkit:SettingsCard
                                    Header="Author :">
                                    <toolkit:SettingsCard.HeaderIcon>
                                        <FontIcon Glyph="&#xE943;"/>
                                    </toolkit:SettingsCard.HeaderIcon>
                                    <TextBlock 
                                    Text="TAN (PI / 2)"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </toolkit:SettingsCard>
                            </toolkit:SettingsExpander.Items>
                        </toolkit:SettingsExpander>

                    </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
